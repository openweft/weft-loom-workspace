#!/usr/bin/env bash
# go wrapper → golang container. Symmetric to pdflatex / marp / pandoc.
# Matches ghcr.io/openweft/weft-loom-golang (note repo name disambiguates
# from a literal `go` binary).
set -euo pipefail
exec crun exec --cwd "$PWD" golang go "$@"
